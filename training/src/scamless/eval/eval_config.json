{
  "macro_f1_min": 0.65,
  "fp_rate_max": 0.06,
  "scam_union_f1_min": 0.80,
  "precision_floor": 0.60,
  "notes": "v0.9 launch gates. precision_floor 0.60: the tuner picks the F1-best threshold among candidates with precision >= 60%. This is calibrated for a model with overlapping categories (phishing/generic_spam share training signal) where 0.90 precision floors pushed thresholds to 0.95 and killed recall to 0.13. Tighten as data grows."
}